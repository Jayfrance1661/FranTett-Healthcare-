===== LAB REQUEST FUNCTIONS =====

===== initializeLabRequestSignature =====
function initializeLabRequestSignature() {

    labSignatureCanvas =
        document.getElementById(
            "labRequestSignaturePad"
        );

    if (!labSignatureCanvas) {
        console.error(
            "Laboratory signature canvas not found."
        );
        return;
    }


    labSignatureContext =
        labSignatureCanvas.getContext("2d");


    // Clear canvas
    labSignatureContext.clearRect(
        0,
        0,
        labSignatureCanvas.width,
        labSignatureCanvas.height
    );


    // White background
    labSignatureContext.fillStyle = "#ffffff";

    labSignatureContext.fillRect(
        0,
        0,
        labSignatureCanvas.width,
        labSignatureCanvas.height
    );


    // Blue signature ink
    labSignatureContext.lineWidth = 2;

    labSignatureContext.lineCap = "round";

    labSignatureContext.lineJoin = "round";

    labSignatureContext.strokeStyle = "#2563EB";


    labSignatureDrawing = false;


    // -------------------------------------------------
    // Mouse events
    // -------------------------------------------------

    labSignatureCanvas.onmousedown =
        function (event) {

            labSignatureDrawing = true;

            labSignatureContext.beginPath();

            labSignatureContext.moveTo(
                event.offsetX,
                event.offsetY
            );
        };


    labSignatureCanvas.onmousemove =
        function (event) {

            if (!labSignatureDrawing) {
                return;
            }

            labSignatureContext.lineTo(
                event.offsetX,
                event.offsetY
            );

            labSignatureContext.stroke();
        };


    labSignatureCanvas.onmouseup =
        function () {

            labSignatureDrawing = false;

            labSignatureContext.closePath();

        };


    labSignatureCanvas.onmouseleave =
        function () {

            labSignatureDrawing = false;

        };


    // -------------------------------------------------
    // Touch events
    // -------------------------------------------------

    labSignatureCanvas.ontouchstart =
        function (event) {

            event.preventDefault();

            const rect =
                labSignatureCanvas.getBoundingClientRect();

            const touch =
                event.touches[0];

            const x =
                touch.clientX - rect.left;

            const y =
                touch.clientY - rect.top;


            labSignatureDrawing = true;

            labSignatureContext.beginPath();

            labSignatureContext.moveTo(
                x,
                y
            );
        };


    labSignatureCanvas.ontouchmove =
        function (event) {

            event.preventDefault();

            if (!labSignatureDrawing) {
                return;
            }

            const rect =
                labSignatureCanvas.getBoundingClientRect();

            const touch =
                event.touches[0];

            const x =
                touch.clientX - rect.left;

            const y =
                touch.clientY - rect.top;


            labSignatureContext.lineTo(
                x,
                y
            );

            labSignatureContext.stroke();
        };


    labSignatureCanvas.ontouchend =
        function (event) {

            event.preventDefault();

            labSignatureDrawing = false;

            labSignatureContext.closePath();

        };


    // -------------------------------------------------
    // Signature status
    // -------------------------------------------------

    const status =
        document.getElementById(
            "labRequestSignatureStatus"
        );

    if (status) {

        status.textContent =
            "Signature not provided";

    }
}

===== clearLabRequestSignature =====
function clearLabRequestSignature() {

    if (
        !labSignatureCanvas ||
        !labSignatureContext
    ) {
        initializeLabRequestSignature();
        return;
    }


    labSignatureContext.clearRect(
        0,
        0,
        labSignatureCanvas.width,
        labSignatureCanvas.height
    );


    // Restore white background
    labSignatureContext.fillStyle =
        "#ffffff";

    labSignatureContext.fillRect(
        0,
        0,
        labSignatureCanvas.width,
        labSignatureCanvas.height
    );


    // Restore blue ink
    labSignatureContext.strokeStyle =
        "#2563EB";


    const status =
        document.getElementById(
            "labRequestSignatureStatus"
        );

    if (status) {

        status.textContent =
            "Signature not provided";

    }
}

===== openAddLabRequestModal =====
function openAddLabRequestModal(
    consultationId = null
) {


    // -------------------------------------------------
    // Patient must be selected
    // -------------------------------------------------

    if (!currentPatientId) {

        alert(
            "Please open a patient profile first."
        );

        return;
    }


    // -------------------------------------------------
    // 1. Use supplied consultation ID if available
    // -------------------------------------------------

    if (
        consultationId !== null &&
        consultationId !== undefined &&
        consultationId !== ""
    ) {

        currentConsultationId =
            Number(consultationId);

    }


    // -------------------------------------------------
    // 2. Recover consultation ID from hidden field
    // -------------------------------------------------

    if (
        !currentConsultationId ||
        Number.isNaN(
            Number(currentConsultationId)
        )
    ) {

        const consultationField =
            document.getElementById(
                "consultationId"
            );


        if (
            consultationField &&
            consultationField.value
        ) {

            currentConsultationId =
                Number(
                    consultationField.value
                );

        }

    }


    // -------------------------------------------------
    // 3. Final consultation check
    // -------------------------------------------------

    if (
        !currentConsultationId ||
        Number.isNaN(
            Number(currentConsultationId)
        )
    ) {

        alert(
            "Please open a consultation before creating a laboratory request."
        );

        return;
    }


    // -------------------------------------------------
    // Normalize consultation ID
    // -------------------------------------------------

    currentConsultationId =
        Number(currentConsultationId);


    console.log(
        "Lab request consultation ID:",
        currentConsultationId
    );


    // -------------------------------------------------
    // Find modal
    // -------------------------------------------------

    const modal =
        document.getElementById(
            "labRequestModal"
        );


    if (!modal) {

        console.error(
            "Laboratory Request modal not found."
        );

        return;
    }


    // -------------------------------------------------
    // Clear previous test selections
    // -------------------------------------------------

    document
        .querySelectorAll(
            ".lab-request-test"
        )
        .forEach(
            function (checkbox) {

                checkbox.checked =
                    false;

            }
        );


    // -------------------------------------------------
    // Clear form fields
    // -------------------------------------------------

    const otherTest =
        document.getElementById(
            "labRequestOtherTest"
        );


    const clinicalInformation =
        document.getElementById(
            "labRequestClinicalInformation"
        );


    const requestedBy =
        document.getElementById(
            "labRequestRequestedBy"
        );


    if (otherTest) {

        otherTest.value = "";

    }


    if (clinicalInformation) {

        clinicalInformation.value = "";

    }


    if (requestedBy) {

        requestedBy.value = "";

    }


    // -------------------------------------------------
    // Initialize electronic signature
    // -------------------------------------------------

    if (
        typeof initializeLabRequestSignature ===
        "function"
    ) {

        initializeLabRequestSignature();

    }


    // -------------------------------------------------
    // Clear electronic signature
    // -------------------------------------------------

    if (
        typeof clearLabRequestSignature ===
        "function"
    ) {

        clearLabRequestSignature();

    }


    // -------------------------------------------------
    // Patient ID
    // -------------------------------------------------

    const patientIdField =
        document.getElementById(
            "labRequestPatientId"
        );


    if (patientIdField) {

        patientIdField.textContent =
            currentPatientId;

    }


    // -------------------------------------------------
    // Patient name
    // -------------------------------------------------

    const patientName =
        document.getElementById(
            "viewName"
        );


    const patientNameField =
        document.getElementById(
            "labRequestPatientName"
        );


    if (patientNameField) {

        patientNameField.textContent =
            patientName
                ? patientName.textContent.trim()
                : "?";

    }


    // -------------------------------------------------
    // Date of birth
    // -------------------------------------------------

    const dob =
        document.getElementById(
            "viewDateOfBirth"
        );


    const dobField =
        document.getElementById(
            "labRequestPatientDob"
        );


    if (dobField) {

        dobField.textContent =
            dob
                ? dob.textContent.trim()
                : "?";

    }


    // -------------------------------------------------
    // Gender
    // -------------------------------------------------

    const gender =
        document.getElementById(
            "viewGender"
        );


    const genderField =
        document.getElementById(
            "labRequestPatientGender"
        );


    if (genderField) {

        genderField.textContent =
            gender
                ? gender.textContent.trim()
                : "?";

    }


    // -------------------------------------------------
    // Current date
    // -------------------------------------------------

    const today =
        new Date()
            .toISOString()
            .split("T")[0];


    const dateDisplay =
        document.getElementById(
            "labRequestDateDisplay"
        );


    const signatureDate =
        document.getElementById(
            "labRequestSignatureDate"
        );


    if (dateDisplay) {

        dateDisplay.textContent =
            today;

    }


    if (signatureDate) {

        signatureDate.textContent =
            today;

    }


    // -------------------------------------------------
    // Store consultation ID in hidden field
    // -------------------------------------------------

    const labConsultationField =
        document.getElementById(
            "labRequestConsultationId"
        );


    if (labConsultationField) {

        labConsultationField.value =
            currentConsultationId;

    }


    // -------------------------------------------------
    // Also synchronize main consultation field
    // -------------------------------------------------

    const consultationField =
        document.getElementById(
            "consultationId"
        );


    if (consultationField) {

        consultationField.value =
            currentConsultationId;

    }


    // -------------------------------------------------
    // Open modal
    // -------------------------------------------------

    modal.style.display =
        "block";


    console.log(
        "Laboratory Request opened successfully. Consultation ID:",
        currentConsultationId
    );

}

===== closeLabRequestModal =====
function closeLabRequestModal() {

    const modal =
        document.getElementById(
            "labRequestModal"
        );


    if (modal) {

        modal.style.display =
            "none";

    }

}

===== saveLabRequest =====
function saveLabRequest() {

    // -------------------------------------------------
    // Make sure a patient is open
    // -------------------------------------------------

    if (!currentPatientId) {

        alert(
            "Please open a patient profile first."
        );

        return;
    }


    // -------------------------------------------------
    // Collect selected laboratory investigations
    // -------------------------------------------------

    const selectedTests = [];

    document
        .querySelectorAll(
            ".lab-request-test:checked"
        )
        .forEach(
            function (checkbox) {

                selectedTests.push(
                    checkbox.value
                );

            }
        );


    // -------------------------------------------------
    // Add "Other Investigation"
    // -------------------------------------------------

    const otherTest =
        document.getElementById(
            "labRequestOtherTest"
        );

    if (
        otherTest &&
        otherTest.value.trim()
    ) {

        selectedTests.push(
            otherTest.value.trim()
        );

    }


    // -------------------------------------------------
    // Require at least one investigation
    // -------------------------------------------------

    if (
        selectedTests.length === 0
    ) {

        alert(
            "Please select at least one laboratory investigation."
        );

        return;
    }


    // -------------------------------------------------
    // Get clinical information
    // -------------------------------------------------

    const clinicalInformation =
        document.getElementById(
            "labRequestClinicalInformation"
        );


    // -------------------------------------------------
    // Get requesting clinician
    // -------------------------------------------------

    const requestedBy =
        document.getElementById(
            "labRequestRequestedBy"
        );


    // -------------------------------------------------
    // Request date
    // -------------------------------------------------

    const requestDate =
        new Date()
            .toISOString()
            .split("T")[0];


    // -------------------------------------------------
    // Get electronic signature
    // -------------------------------------------------

    const signatureCanvas =
        document.getElementById(
            "labRequestSignaturePad"
        );

    let electronicSignature = "";


    if (signatureCanvas) {

        electronicSignature =
            signatureCanvas.toDataURL(
                "image/png"
            );

    }


    // -------------------------------------------------
    // Make sure we actually captured the signature
    // -------------------------------------------------

    if (
        !electronicSignature ||
        electronicSignature.length < 100
    ) {

        alert(
            "Please provide an electronic signature before saving the laboratory request."
        );

        return;
    }


    // -------------------------------------------------
    // Prepare laboratory request
    // -------------------------------------------------

    const consultationId =
    currentConsultationId || null;

const labRequestData = {

    consultation_id:
    currentConsultationId || null,

    requested_tests:
        selectedTests.join(", "),

        clinical_information:
            clinicalInformation
                ? clinicalInformation.value.trim()
                : "",

        requested_by:
            requestedBy
                ? requestedBy.value.trim()
                : "",

        request_date:
            requestDate,

        electronic_signature:
            electronicSignature

    };


    // -------------------------------------------------
    // Send request to server
    // -------------------------------------------------

    try {

    const token = getToken();

    if (!token) {
        throw new Error(
            "Authentication required. Please log in again."
        );
    }

    const response =
        await fetch(
            `${API_URL}/api/patients/${currentPatientId}/lab-requests`,
            {
                method: "POST",

                headers: {
                    "Content-Type":
                        "application/json",

                    "Authorization":
                        `Bearer ${token}`
                },

                body:
                    JSON.stringify(
                        labRequestData
                    )
            }
        );

        // -------------------------------------------------
        // Read server response
        // -------------------------------------------------

        const data =
            await response.json();


        // -------------------------------------------------
        // Handle server error
        // -------------------------------------------------

        if (!response.ok) {

            throw new Error(
                data.message ||
                "Failed to save laboratory request."
            );

        }


        // -------------------------------------------------
        // Success
        // -------------------------------------------------

        alert(
            "Laboratory request saved successfully."
        );


        // -------------------------------------------------
        // Close laboratory request modal
        // -------------------------------------------------

        closeLabRequestModal();

        await loadLaboratoryRequests(currentPatientId);

    } catch (error) {

        console.error(
            "Save laboratory request error:",
            error
        );

        alert(
            error.message ||
            "Unable to save laboratory request."
        );

    }

}

===== printLabRequest =====
function printLabRequest() {

    const printable =
        document.getElementById("labRequestPrintable");

    if (!printable) {
        alert("Laboratory request form not found.");
        return;
    }

    const printWindow =
        window.open(
            "",
            "_blank",
            "width=900,height=1100"
        );

    if (!printWindow) {
        alert(
            "Please allow pop-ups for printing."
        );
        return;
    }

    const printClone =
    printable.cloneNode(true);

const originalCheckboxes =
    printable.querySelectorAll(
        ".lab-request-test"
    );

const clonedCheckboxes =
    printClone.querySelectorAll(
        ".lab-request-test"
    );

originalCheckboxes.forEach(
    function (originalCheckbox, index) {

        const clonedCheckbox =
            clonedCheckboxes[index];

        if (!clonedCheckbox) {
            return;
        }

        const label =
            clonedCheckbox.closest("label");

        if (!label) {
            return;
        }

        const testName =
            label.textContent.trim();

        /*
         * Remove the real checkbox because
         * browsers may not print its checked state.
         */
        clonedCheckbox.remove();

        /*
         * Create a visible printed checkbox.
         */
        const printedCheckbox =
            document.createElement("span");

        printedCheckbox.textContent =
            originalCheckbox.checked
                ? "? "
                : "? ";

        printedCheckbox.style.fontSize =
            "14px";

        printedCheckbox.style.fontWeight =
            "bold";

        /*
         * Replace the label content with
         * the visible checkbox + test name.
         */
        label.textContent = "";

        label.appendChild(
            printedCheckbox
        );

        label.appendChild(
            document.createTextNode(testName)
        );
    }
);

// =====================================================
// CONVERT ELECTRONIC SIGNATURE CANVAS TO IMAGE
// =====================================================

const originalSignatureCanvas =
    printable.querySelector(
        "#labRequestSignaturePad"
    );

const clonedSignatureCanvas =
    printClone.querySelector(
        "#labRequestSignaturePad"
    );

if (
    originalSignatureCanvas &&
    clonedSignatureCanvas
) {

    const signatureImage =
        originalSignatureCanvas.toDataURL(
            "image/png"
        );

    const signatureImg =
        document.createElement("img");

    signatureImg.src =
        signatureImage;

    signatureImg.alt =
        "Electronic Signature";

    signatureImg.style.width =
        "500px";

    signatureImg.style.height =
        "160px";

    signatureImg.style.objectFit =
        "contain";

    signatureImg.style.display =
        "block";

    signatureImg.style.background =
        "#ffffff";

    clonedSignatureCanvas.replaceWith(
        signatureImg
    );
}


// Remove the Clear Signature button
const clonedSignatureActions =
    printClone.querySelector(
        ".signature-actions"
    );

if (clonedSignatureActions) {
    clonedSignatureActions.remove();
}


// Remove signature status text
const clonedSignatureStatus =
    printClone.querySelector(
        "#labRequestSignatureStatus"
    );

if (clonedSignatureStatus) {
    clonedSignatureStatus.remove();
}

const printContent =
    printClone.innerHTML;

    const printHtml =
        "<!DOCTYPE html>" +
        "<html>" +
        "<head>" +

        "<meta charset='UTF-8'>" +

        "<title>" +
        "Laboratory Request - FranTett Healthcare" +
        "</title>" +

        "<style>" +

        "@page {" +
            "size: A4;" +
            "margin: 14mm;" +
        "}" +

        "* {" +
            "box-sizing: border-box;" +
        "}" +

        "html, body {" +
            "margin: 0;" +
            "padding: 0;" +
            "background: #fff;" +
            "color: #222;" +
            "font-family: Arial, Helvetica, sans-serif;" +
            "font-size: 12px;" +
        "}" +

        "body {" +
            "width: 100%;" +
        "}" +

        ".lab-request-header {" +
            "text-align: center;" +
            "border-bottom: 2px solid #222;" +
            "padding-bottom: 12px;" +
            "margin-bottom: 18px;" +
        "}" +

        ".lab-request-header h1 {" +
            "margin: 0;" +
            "font-size: 24px;" +
        "}" +

        ".lab-request-header p {" +
            "margin: 5px 0 10px;" +
            "font-size: 12px;" +
        "}" +

        ".lab-request-header h2 {" +
            "margin: 0;" +
            "font-size: 18px;" +
        "}" +

        ".lab-request-patient-section," +
        ".lab-request-section {" +
            "margin-bottom: 18px;" +
        "}" +

        ".lab-request-patient-section h3," +
        ".lab-request-section h3 {" +
            "margin: 0 0 8px;" +
            "font-size: 14px;" +
            "border-bottom: 1px solid #aaa;" +
            "padding-bottom: 4px;" +
        "}" +

        ".lab-request-patient-grid {" +
            "display: grid;" +
            "grid-template-columns: 1fr 1fr;" +
            "gap: 8px 20px;" +
        "}" +

        ".lab-request-tests {" +
            "display: grid;" +
            "grid-template-columns: 1fr 1fr;" +
            "gap: 7px 15px;" +
        "}" +

        ".lab-request-tests label {" +
            "display: block;" +
        "}" +

        ".lab-request-tests input {" +
            "margin-right: 6px;" +
        "}" +

        ".form-group {" +
            "margin-top: 10px;" +
        "}" +

        ".form-group label {" +
            "display: block;" +
            "font-weight: bold;" +
            "margin-bottom: 5px;" +
        "}" +

        "input[type='text']," +
        "textarea {" +
            "width: 100%;" +
            "border: 1px solid #999;" +
            "padding: 7px;" +
            "font-family: Arial, Helvetica, sans-serif;" +
            "font-size: 12px;" +
            "background: #fff;" +
        "}" +

        "textarea {" +
            "min-height: 70px;" +
            "resize: none;" +
        "}" +

        ".lab-request-signature {" +
            "display: flex;" +
            "flex-direction: column;" +
            "align-items: flex-start;" +
            "margin-top: 20px;" +
        "}" +

        ".signature-line {" +
            "border-bottom: 1px solid #222;" +
            "height: 28px;" +
            "margin-top: 5px;" +
        "}" +

        ".lab-request-footer {" +
            "margin-top: 40px;" +
            "padding-top: 10px;" +
            "border-top: 1px solid #aaa;" +
            "text-align: center;" +
            "font-size: 10px;" +
            "line-height: 1.5;" +
        "}" +

        ".no-print," +
        "button," +
        ".close-modal {" +
            "display: none !important;" +
        "}" +

        "</style>" +


        "</head>" +

        "<body>" +
        "<div id='labRequestPrintable'>" +
            printContent +
        "</div>" +

        "<\/body>" +
        "<\/html>";

    printWindow.document.open();

    printWindow.document.write(
        printHtml
    );

    printWindow.document.close();

    printWindow.focus();

    setTimeout(
        function () {

            printWindow.print();

        },
        500
    );
}

===== LAB REQUEST CSS =====

<style>

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
            background: #f5f7fa;
            color: #222;
        }

        nav {
            background: #0b6b4f;
            color: white;
            padding: 16px 25px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 15px;
        }

        nav .logo {
            font-size: 22px;
            font-weight: bold;
        }

        nav .links {
            display: flex;
            gap: 18px;
            flex-wrap: wrap;
        }

        nav a {
            color: white;
            text-decoration: none;
            font-weight: 500;
        }

        nav a:hover {
            text-decoration: underline;
        }

        .container {
            width: 95%;
            max-width: 1400px;
            margin: 30px auto;
        }

        h1 {
            margin-bottom: 5px;
        }

        .subtitle {
            color: #666;
            margin-bottom: 25px;
        }

        .top-actions {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 15px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }

        button {
            border: none;
            cursor: pointer;
            border-radius: 5px;
            padding: 10px 16px;
            font-size: 14px;
        }

        .primary-btn,
        .save-btn {
            background: #0b6b4f;
            color: white;
        }

        .primary-btn:hover,
        .save-btn:hover {
            background: #09563f;
        }

        .cancel-btn {
            background: #777;
            color: white;
        }

        .danger-btn {
            background: #c62828;
            color: white;
        }

        .view-btn {
            background: #1976d2;
            color: white;
        }

        .edit-btn {
            background: #f39c12;
            color: white;
        }

        .search-box {
            width: 100%;
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-bottom: 20px;
            font-size: 15px;
        }

        .table-wrapper {
            background: white;
            border-radius: 8px;
            overflow-x: auto;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th,
        td {
            padding: 13px;
            border-bottom: 1px solid #eee;
            text-align: left;
            vertical-align: top;
        }

        th {
            background: #f0f3f5;
            font-weight: bold;
        }

        .actions {
            display: flex;
            gap: 6px;
            flex-wrap: wrap;
        }

        .loading {
            text-align: center;
            padding: 30px;
            color: #666;
        }

        .message {
            display: none;
            padding: 12px 15px;
            margin-bottom: 20px;
            border-radius: 5px;
        }

        .message.success {
            background: #dff5e8;
            color: #176b3a;
        }

        .message.error {
            background: #fde2e2;
            color: #a71919;
        }

        /* ==========================
           MODAL
        ========================== */

        .modal {
            display: none;
            position: fixed;
            z-index: 9999;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.55);
            overflow-y: auto;
            padding: 30px 15px;
        }

        
/* Hide original renderer headings inside Patient Profile.
   Keep the new colored department headings visible. */

.patient-profile-modal .clinical-department-vitals h3,
.patient-profile-modal .clinical-department-lab-requests h3,
.patient-profile-modal .clinical-department-lab-results h3,
.patient-profile-modal .clinical-department-lab-reports h3,
.patient-profile-modal .clinical-department-prescriptions h3 {
    display: none;
}

/* Remove duplicate renderer headings.
   Keep only the new colored department header. */

.patient-profile-modal .clinical-department h3,
.patient-profile-modal .clinical-department h4 {
    display: none;
}

.patient-profile-modal .clinical-department > .clinical-department-header h4 {
    display: block;
}

/* Hide old renderer department headings in Patient Profile.
   Keep the new colored department headings. */

.patient-profile-modal .clinical-department .clinical-subsection-title {
    display: none !important;
}

/* Professional styling for Patient Profile consultation detail headings */

.patient-profile-modal .clinical-item > strong {
    display: block;
    color: #2e7d32;
    background: #edf7ee;
    border-left: 4px solid #43a047;
    padding: 8px 12px;
    margin-bottom: 6px;
    border-radius: 5px;
    font-weight: 700;
}

.patient-profile-modal .clinical-item {
    margin-bottom: 12px;
}

/* Professional consultation detail labels in Patient Profile */

.patient-profile-modal .clinical-detail-label {
    display: block !important;
    color: #2e7d32 !important;
    background: #edf7ee !important;
    border-left: 4px solid #43a047 !important;
    padding: 8px 12px !important;
    margin-bottom: 6px !important;
    border-radius: 5px !important;
    font-weight: 700 !important;
}


/* Professional green clinical table headings */

.patient-profile-modal .clinical-table thead th {
    color: #2e7d32 !important;
    background: #edf7ee !important;
    border-top: 1px solid #a5d6a7 !important;
    border-bottom: 2px solid #43a047 !important;
    font-weight: 700 !important;
    padding: 10px 12px !important;
}
/* =====================================================
   PATIENT PROFILE CLINICAL TABLES
===================================================== */

.patient-profile-modal .clinical-table {
    width: 100% !important;
    border-collapse: separate !important;
    border-spacing: 0 !important;
    table-layout: auto !important;
    margin-top: 8px !important;
    background: #ffffff !important;
}

.patient-profile-modal .clinical-table th {
    padding: 10px 12px !important;
    text-align: left !important;
    white-space: nowrap !important;
    font-weight: 700 !important;
    border: 1px solid #d9e2e7 !important;
    background: #f4f7f8 !important;
}

.patient-profile-modal .clinical-table td {
    padding: 10px 12px !important;
    text-align: left !important;
    vertical-align: top !important;
    border: 1px solid #d9e2e7 !important;
    line-height: 1.4 !important;
}

.patient-profile-modal .clinical-table th + th,
.patient-profile-modal .clinical-table td + td {
    border-left: 1px solid #aab7bd !important;
}

.patient-profile-modal .clinical-table tbody tr + tr td {
    border-top: 0 !important;
}

/* Laboratory report spacing */

.patient-profile-modal .clinical-lab-report {
    margin-bottom: 18px !important;
}

.patient-profile-modal .clinical-lab-report-title {
    margin-top: 10px !important;
    margin-bottom: 8px !important;
    font-size: 16px !important;
    font-weight: 700 !important;
    color: #3949ab !important;
}

.patient-profile-modal .clinical-lab-report .record-meta {
    margin-bottom: 8px !important;
    line-height: 1.6 !important;
}
/* =====================================================
           CLINICAL CHILD MODALS
           Must appear above Add Consultation
           ===================================================== */

        #vitalSignsModal,
        #labRequestModal,
        #labReportModal,
        #prescriptionFormModal {
            z-index: 10001 !important;
        }


        .modal-content {
            background: white;
            width: 100%;
            max-width: 900px;
            margin: auto;
            border-radius: 8px;
            padding: 25px;
            position: relative;
        }

        .large-modal {
            max-width: 1100px;
        }

        .close-modal {
            position: absolute;
            right: 20px;
            top: 12px;
            font-size: 30px;
            cursor: pointer;
            color: #555;
        }

        .close-modal:hover {
            color: #000;
        }

        .form-group {
            margin-bottom: 16px;
        }

        .form-group label {
            display: block;
            font-weight: bold;
            margin-bottom: 6px;
        }

        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 11px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-family: inherit;
            font-size: 14px;
        }

        textarea {
            min-height: 90px;
            resize: vertical;
        }

        .form-actions {
            display: flex;
            gap: 10px;
            margin-top: 25px;
        }

        /* =====================================================
           PATIENT PROFILE - PROFESSIONAL MEDICAL RECORD DESIGN
        ===================================================== */

        .patient-profile-modal {
            background: #ffffff;
            border-radius: 14px;
            padding: 28px;
        }

        .patient-profile-modal > h2 {
            margin: 0 0 18px 0;
            font-size: 26px;
            font-weight: 700;
            color: #173f35;
            border-bottom: 1px solid #e5ebe8;
            padding-bottom: 14px;
        }

        .patient-profile-modal .top-actions {
            display: flex;
            justify-content: flex-end;
            align-items: center;
            gap: 10px;
            margin-bottom: 18px;
        }

        .patient-header {
            background: linear-gradient(135deg, #f0f8f5, #f8fbfa);
            border: 1px solid #dce9e4;
            border-radius: 12px;
            padding: 22px;
            margin-bottom: 22px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
        }

        .patient-header > h2 {
            margin: 0 0 20px 0;
            font-size: 25px;
            font-weight: 700;
            color: #173f35;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
            gap: 14px;
            margin-top: 18px;
        }

        .info-card {
            background: #ffffff;
            border: 1px solid #e2e9e6;
            border-radius: 9px;
            padding: 15px 16px;
            min-height: 72px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            gap: 6px;
            box-shadow: 0 1px 4px rgba(0, 0, 0, 0.03);
        }

        .info-card strong {
            display: block;
            font-size: 12px;
            font-weight: 700;
            color: #60736d;
            text-transform: uppercase;
            letter-spacing: 0.35px;
        }

        .info-card span {
            display: block;
            color: #1d2d28;
            font-size: 15px;
            font-weight: 500;
            word-break: break-word;
        }

        .patient-header > .info-grid:first-of-type {
            margin-top: 0;
            margin-bottom: 8px;
        }

        .patient-header > .info-grid:first-of-type .info-card {
            min-height: 88px;
            text-align: center;
            align-items: center;
            background: #ffffff;
            border-color: #d8e7e1;
        }

        .patient-header > .info-grid:first-of-type .info-card span {
            font-size: 25px;
            font-weight: 700;
            color: #17624f;
        }

        .patient-profile-modal .section {
            background: #ffffff;
            border: 1px solid #e1e8e5;
            border-radius: 12px;
            padding: 22px;
            margin-top: 22px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.035);
        }

        .patient-profile-modal .section > h3 {
            margin: 0 0 18px 0;
            padding-bottom: 12px;
            border-bottom: 1px solid #e6ece9;
            color: #173f35;
            font-size: 19px;
            font-weight: 700;
        }

        .clinical-item {
            background: #f8faf9;
            border: 1px solid #e5ebe8;
            border-radius: 9px;
            padding: 16px;
            margin-bottom: 14px;
        }

        .clinical-item:last-child {
            margin-bottom: 0;
        }

        .clinical-item > strong {
            display: block;
            margin-bottom: 9px;
            color: #31554b;
            font-size: 13px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.3px;
        }

        .history-box {
            background: #ffffff;
            border: 1px solid #e1e7e4;
            border-radius: 7px;
            padding: 12px 14px;
            min-height: 45px;
            color: #273a34;
            line-height: 1.6;
            font-size: 14px;
        }

        /* =====================================================
           PATIENT PROFILE - VERSION 2 DEPARTMENT DESIGN
        ===================================================== */

        .patient-profile-modal {
            background: #f6f8f9;
            border-radius: 14px;
            padding: 28px;
        }

        .patient-profile-modal > h2 {
            margin: 0 0 18px 0;
            font-size: 26px;
            font-weight: 700;
            color: #173f35;
            border-bottom: 1px solid #dfe7e4;
            padding-bottom: 14px;
        }

        .patient-profile-modal .top-actions {
            display: flex;
            justify-content: flex-end;
            align-items: center;
            gap: 10px;
            margin-bottom: 18px;
        }

        /* =========================
           PATIENT OVERVIEW
        ========================= */

        .patient-header {
            background: #ffffff;
            border: 1px solid #cfe2da;
            border-left: 5px solid #18815f;
            border-radius: 12px;
            padding: 22px;
            margin-bottom: 22px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.04);
        }

        .patient-header > h2 {
            margin: 0 0 20px 0;
            font-size: 25px;
            font-weight: 700;
            color: #17624f;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
            gap: 14px;
            margin-top: 18px;
        }

        .info-card {
            background: #ffffff;
            border: 1px solid #e1e7e4;
            border-radius: 9px;
            padding: 15px 16px;
            min-height: 72px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            gap: 6px;
            box-shadow: 0 1px 4px rgba(0,0,0,0.03);
        }

        .info-card strong {
            display: block;
            font-size: 12px;
            font-weight: 700;
            color: #60736d;
            text-transform: uppercase;
            letter-spacing: .35px;
        }

        .info-card span {
            display: block;
            color: #1d2d28;
            font-size: 15px;
            font-weight: 500;
            word-break: break-word;
        }

        .patient-header > .info-grid:first-of-type {
            margin-top: 0;
            margin-bottom: 8px;
        }

        .patient-header > .info-grid:first-of-type .info-card {
            min-height: 88px;
            text-align: center;
            align-items: center;
            border-color: #cfe2da;
            background: #f7fbf9;
        }

        .patient-header > .info-grid:first-of-type .info-card span {
            font-size: 25px;
            font-weight: 700;
            color: #17624f;
        }

        /* =========================
           GENERAL SECTIONS
        ========================= */

        .patient-profile-modal .section {
            background: #ffffff;
            border: 1px solid #e1e7e4;
            border-radius: 12px;
            padding: 22px;
            margin-top: 22px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.035);
        }

        .patient-profile-modal .section > h3 {
            margin: 0 0 18px 0;
            padding: 12px 14px;
            border-radius: 8px;
            border-left: 5px solid #60736d;
            background: #f5f7f6;
            color: #263a34;
            font-size: 19px;
            font-weight: 700;
        }

        /* =========================
           CLINICAL INFORMATION
        ========================= */

        .patient-profile-modal .section:has(#viewMedicalHistory) > h3 {
            border-left-color: #18815f;
            background: #f0f8f5;
            color: #17624f;
        }

        .clinical-item {
            background: #f8faf9;
            border: 1px solid #e1e8e5;
            border-radius: 9px;
            padding: 16px;
            margin-bottom: 14px;
        }

        .clinical-item:last-child {
            margin-bottom: 0;
        }

        .clinical-item > strong {
            display: block;
            margin-bottom: 9px;
            color: #31554b;
            font-size: 13px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: .3px;
        }

        .history-box {
            background: #ffffff;
            border: 1px solid #e1e7e4;
            border-radius: 7px;
            padding: 12px 14px;
            min-height: 45px;
            color: #273a34;
            line-height: 1.6;
            font-size: 14px;
        }

        /* =========================
           VITAL SIGNS - BLUE
        ========================= */

        .patient-profile-modal .section:has(#vitalSignsHistory) {
            border-top: 4px solid #2878b8;
        }

        .patient-profile-modal .section:has(#vitalSignsHistory) > .top-actions h3 {
            border-left-color: #2878b8;
            background: #eef6fc;
            color: #205f91;
        }

        /* =========================
           APPOINTMENTS - PURPLE
        ========================= */

        .patient-profile-modal .section:has(#appointmentHistory) {
            border-top: 4px solid #7656a6;
        }

        .patient-profile-modal .section:has(#appointmentHistory) > h3 {
            border-left-color: #7656a6;
            background: #f5f1fa;
            color: #60438e;
        }

        /* =========================
           PATIENT TIMELINE - SLATE
        ========================= */

        .patient-profile-modal .section:has(#patientTimeline) {
            border-top: 4px solid #526777;
            background: #fbfcfd;
        }

        .patient-profile-modal .section:has(#patientTimeline) > h3 {
            border-left-color: #526777;
            background: #f0f4f7;
            color: #405564;
        }

        #patientTimeline {
            position: relative;
            padding-left: 18px;
        }

        /* =========================
           CONSULTATIONS - ORANGE
        ========================= */

        .patient-profile-modal .section:has(#consultationHistory) {
            border-top: 4px solid #d87928;
        }

        .patient-profile-modal .section:has(#consultationHistory) > .top-actions h3 {
            border-left-color: #d87928;
            background: #fff5eb;
            color: #a95d20;
        }

        /* =========================
           LAB RESULTS - RED
        ========================= */

        .patient-profile-modal .section:has(#labResultsHistory) {
            border-top: 4px solid #c74b4b;
        }

        .patient-profile-modal .section:has(#labResultsHistory) > .top-actions h3 {
            border-left-color: #c74b4b;
            background: #fff1f1;
            color: #a43d3d;
        }

        /* =========================
           LAB REQUESTS - TEAL
        ========================= */

        .patient-profile-modal .section:has(#labRequestsHistory) {
            border-top: 4px solid #159a9c;
        }

        .patient-profile-modal .section:has(#labRequestsHistory) > .top-actions h3 {
            border-left-color: #159a9c;
            background: #edfafa;
            color: #117578;
        }

        /* =========================
           PRESCRIPTIONS - AMBER
        ========================= */

        .patient-profile-modal .section:has(#prescriptionHistory) {
            border-top: 4px solid #c99425;
        }

        .patient-profile-modal .section:has(#prescriptionHistory) > .top-actions h3 {
            border-left-color: #c99425;
            background: #fff9e9;
            color: #966f18;
        }

        /* =========================
           DEPARTMENT HOVER EFFECT
        ========================= */

        .patient-profile-modal .section {
            transition:
                transform 0.2s ease,
                box-shadow 0.2s ease,
                border-color 0.2s ease;
        }

        .patient-profile-modal .section:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.10);
        }

        /* Vital Signs */
        .patient-profile-modal .section:has(#vitalSignsHistory):hover {
            border-top-color: #1d6fa5;
        }

        /* Appointments */
        .patient-profile-modal .section:has(#appointmentHistory):hover {
            border-top-color: #60438e;
        }

        /* Timeline */
        .patient-profile-modal .section:has(#patientTimeline):hover {
            border-top-color: #405564;
        }

        /* Consultations */
        .patient-profile-modal .section:has(#consultationHistory):hover {
            border-top-color: #b85f1c;
        }

        /* Laboratory Results */
        .patient-profile-modal .section:has(#labResultsHistory):hover {
            border-top-color: #a43d3d;
        }

        /* Laboratory Requests */
        .patient-profile-modal .section:has(#labRequestsHistory):hover {
            border-top-color: #117578;
        }

        /* Prescriptions */
        .patient-profile-modal .section:has(#prescriptionHistory):hover {
            border-top-color: #966f18;
        }
        /* =========================
           BUTTON ALIGNMENT
        ========================= */
        /* =========================
           LAB REPORT HEADER LAYOUT
        ========================= */

        .lab-report-card .lab-report-header {
            display: block;
            position: relative;
            padding-bottom: 14px;
            margin-bottom: 16px;
            border-bottom: 1px solid #eee1e1;
        }

        .lab-report-card .lab-report-date {
            display: block;
            margin-bottom: 4px;
            font-size: 14px;
            font-weight: 600;
            color: #5f6865;
        }

        .lab-report-card .lab-report-header h4 {
            margin: 0 0 10px 0;
            font-size: 18px;
            color: #a43d3d;
        }

        .lab-report-card .lab-report-meta {
            display: flex;
            align-items: center;
            gap: 10px;
            padding-right: 150px;
            flex-wrap: wrap;
        }

        .lab-report-card .lab-report-header > .danger-btn {
            position: absolute;
            right: 0;
            bottom: 14px;
            margin: 0;
            white-space: nowrap;
        }

        @media (max-width: 700px) {
            .lab-report-card .lab-report-header > .danger-btn {
                position: static;
                margin-top: 10px;
                width: 100%;
            }

            .lab-report-card .lab-report-meta {
                padding-right: 0;
            }
        }


        .patient-profile-modal .section .top-actions {
            justify-content: space-between;
            margin-bottom: 14px;
        }

        .patient-profile-modal .section .top-actions h3 {
            flex: 1;
            margin: 0;
        }

        @media (max-width: 700px) {

            .patient-profile-modal {
                padding: 18px;
            }

            .patient-header {
                padding: 16px;
            }

            .info-grid {
                grid-template-columns: 1fr;
            }

            .patient-profile-modal .top-actions {
                justify-content: stretch;
                flex-wrap: wrap;
            }

            .patient-profile-modal .top-actions button {
                width: 100%;
            }

            .patient-profile-modal .section .top-actions {
                flex-direction: column;
                align-items: stretch;
            }

        }
        @media (max-width: 700px) {
            .patient-profile-modal {
                padding: 18px;
            }

            .patient-header {
                padding: 16px;
            }

            .info-grid {
                grid-template-columns: 1fr;
            }

            .patient-profile-modal .top-actions {
                justify-content: stretch;
            }

            .patient-profile-modal .top-actions button {
                width: 100%;
            }
        }

        .info-card strong {
            display: block;
            margin-bottom: 5px;
            color: #555;
        }

        .section {
            margin-top: 25px;
        }

        .section h3 {
            border-bottom: 2px solid #0b6b4f;
            padding-bottom: 8px;
        }

        .history-box {
            background: #fafafa;
            border: 1px solid #eee;
            padding: 15px;
            border-radius: 5px;
            white-space: pre-wrap;
        }

        /* ==========================
           CONSULTATIONS
        ========================== */

        .consultation-card {
            border: 1px solid #ddd;
            border-radius: 7px;
            padding: 18px;
            margin-bottom: 15px;
            background: white;
        }

        .consultation-title {
            display: flex;
            justify-content: space-between;
            gap: 10px;
            flex-wrap: wrap;
            margin-bottom: 15px;
        }

        .consultation-title h4 {
            margin: 0;
        }

        .consultation-actions {
            display: flex;
            gap: 6px;
            flex-wrap: wrap;
        }

        .clinical-item {
            margin-bottom: 12px;
        }

.clinical-date {
    margin-bottom: 12px;
}

        .clinical-item strong {
            display: block;
            margin-bottom: 4px;
            color: #444;
        }

        .clinical-value {
            white-space: pre-wrap;
            background: #fafafa;
            border-radius: 4px;
            padding: 8px;
        }

        .no-records {
            background: #fafafa;
            padding: 18px;
            border-radius: 5px;
            color: #666;
        }

                .info-card strong {
            display: block;
            font-size: 12px;
            font-weight: 700;
            color: #60736d;
            text-transform: uppercase;
            letter-spacing: .35px;
        }

        .info-card span {
            display: block;
            color: #1d2d28;
            font-size: 15px;
            font-weight: 500;
            word-break: break-word;
        }

        .patient-header > .info-grid:first-of-type {
            margin-top: 0;
            margin-bottom: 8px;
        }

        .patient-header > .info-grid:first-of-type .info-card {
            min-height: 88px;
            text-align: center;
            align-items: center;
            border-color: #cfe2da;
            background: #f7fbf9;
        }

        .patient-header > .info-grid:first-of-type .info-card span {
            font-size: 25px;
            font-weight: 700;
            color: #17624f;
        }

        /* =========================
           GENERAL SECTIONS
        ========================= */

        .patient-profile-modal .section {
            background: #ffffff;
            border: 1px solid #e1e7e4;
            border-radius: 12px;
            padding: 22px;
            margin-top: 22px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.035);
        }

        .patient-profile-modal .section > h3 {
            margin: 0 0 18px 0;
            padding: 12px 14px;
            border-radius: 8px;
            border-left: 5px solid #60736d;
            background: #f5f7f6;
            color: #263a34;
            font-size: 19px;
            font-weight: 700;
        }

        /* =========================
           CLINICAL INFORMATION
        ========================= */

        .patient-profile-modal .section:has(#viewMedicalHistory) > h3 {
            border-left-color: #18815f;
            background: #f0f8f5;
            color: #17624f;
        }

        .clinical-item {
            background: #f8faf9;
            border: 1px solid #e1e8e5;
            border-radius: 9px;
            padding: 16px;
            margin-bottom: 14px;
        }

        .clinical-item:last-child {
            margin-bottom: 0;
        }

        .clinical-item > strong {
            display: block;
            margin-bottom: 9px;
            color: #31554b;
            font-size: 13px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: .3px;
        }

        .history-box {
            background: #ffffff;
            border: 1px solid #e1e7e4;
            border-radius: 7px;
            padding: 12px 14px;
            min-height: 45px;
            color: #273a34;
            line-height: 1.6;
            font-size: 14px;
        }

        /* =========================
           VITAL SIGNS - BLUE
        ========================= */

        .patient-profile-modal .section:has(#vitalSignsHistory) {
            border-top: 4px solid #2878b8;
        }

        .patient-profile-modal .section:has(#vitalSignsHistory) > .top-actions h3 {
            border-left-color: #2878b8;
            background: #eef6fc;
            color: #205f91;
        }

        /* =========================
           APPOINTMENTS - PURPLE
        ========================= */

        .patient-profile-modal .section:has(#appointmentHistory) {
            border-top: 4px solid #7656a6;
        }

        .patient-profile-modal .section:has(#appointmentHistory) > h3 {
            border-left-color: #7656a6;
            background: #f5f1fa;
            color: #60438e;
        }

        /* =========================
           PATIENT TIMELINE - SLATE
        ========================= */

        .patient-profile-modal .section:has(#patientTimeline) {
            border-top: 4px solid #526777;
            background: #fbfcfd;
        }

        .patient-profile-modal .section:has(#patientTimeline) > h3 {
            border-left-color: #526777;
            background: #f0f4f7;
            color: #405564;
        }

        #patientTimeline {
            position: relative;
            padding-left: 18px;
        }

        /* =========================
           CONSULTATIONS - ORANGE
        ========================= */

        .patient-profile-modal .section:has(#consultationHistory) {
            border-top: 4px solid #d87928;
        }

        .patient-profile-modal .section:has(#consultationHistory) > .top-actions h3 {
            border-left-color: #d87928;
            background: #fff5eb;
            color: #a95d20;
        }

        /* =========================
           LAB RESULTS - RED
        ========================= */

        .patient-profile-modal .section:has(#labResultsHistory) {
            border-top: 4px solid #c74b4b;
        }

        .patient-profile-modal .section:has(#labResultsHistory) > .top-actions h3 {
            border-left-color: #c74b4b;
            background: #fff1f1;
            color: #a43d3d;
        }

        /* =========================
           LAB REQUESTS - TEAL
        ========================= */

        .patient-profile-modal .section:has(#labRequestsHistory) {
            border-top: 4px solid #159a9c;
        }

        .patient-profile-modal .section:has(#labRequestsHistory) > .top-actions h3 {
            border-left-color: #159a9c;
            background: #edfafa;
            color: #117578;
        }

        /* =========================
           PRESCRIPTIONS - AMBER
        ========================= */

        .patient-profile-modal .section:has(#prescriptionHistory) {
            border-top: 4px solid #c99425;
        }

        .patient-profile-modal .section:has(#prescriptionHistory) > .top-actions h3 {
            border-left-color: #c99425;
            background: #fff9e9;
            color: #966f18;
        }

        /* =========================
           BUTTON ALIGNMENT
        ========================= */

        .patient-profile-modal .section .top-actions {
            justify-content: space-between;
            margin-bottom: 14px;
        }

        .patient-profile-modal .section .top-actions h3 {
            flex: 1;
            margin: 0;
        }

        @media (max-width: 700px) {

            .patient-profile-modal {
                padding: 18px;
            }

            .patient-header {
                padding: 16px;
            }

            .info-grid {
                grid-template-columns: 1fr;
            }

            .patient-profile-modal .top-actions {
                justify-content: stretch;
                flex-wrap: wrap;
            }

            .patient-profile-modal .top-actions button {
                width: 100%;
            }

            .patient-profile-modal .section .top-actions {
                flex-direction: column;
                align-items: stretch;
            }

        }
        @media (max-width: 700px) {

            nav {
                align-items: flex-start;
            }

            nav .links {
                flex-direction: column;
                gap: 8px;
            }

            .modal {
                padding: 10px;
            }

            .modal-content {
                padding: 18px;
            }

            th,
            td {
                padding: 9px;
            }

        }

@media print {

    @page {
        size: A4;
        margin: 10mm;
    }

    body {
        background: white !important;
        color: black !important;
        font-size: 11px;
    }

    body > nav,
    body > .container {
        display: none !important;
    }

    #viewPatientModal {
        display: block !important;
        position: static !important;
        width: 100% !important;
        height: auto !important;
        padding: 0 !important;
        background: white !important;
        overflow: visible !important;
    }

    #viewPatientModal .modal-content {
        display: block !important;
        width: 100% !important;
        max-width: none !important;
        margin: 0 !important;
        padding: 0 !important;
        border: none !important;
        box-shadow: none !important;
    }

    .close-modal,
    button,
    .form-actions,
    #patientFormModal,
    #prescriptionFormModal,
    #consultationFormModal,
    #viewConsultationModal {
        display: none !important;
    }

    /* Print heading */
    #viewPatientModal .modal-content::before {
        content: "FRANTETT HEALTHCARE";
        font-size: 18px;
        white-space: pre;
        display: block;
        text-align: center;
        font-size: 18px;
        font-weight: bold;
        margin-bottom: 12px;
        border-bottom: 2px solid #000;
        padding-bottom: 8px;
    }

    .patient-header {
        background: white !important;
        border: 1px solid #999 !important;
        padding: 10px !important;
        margin-bottom: 10px !important;
    }

    .patient-header h2 {
        margin: 0 0 8px 0 !important;
        font-size: 17px;
    }

    .info-grid {
        display: grid !important;
        grid-template-columns: repeat(2, 1fr) !important;
        gap: 5px !important;
    }

    .info-card {
        background: white !important;
        border: 1px solid #bbb !important;
        padding: 6px !important;
    }

    .section {
        margin-top: 10px !important;
    }

    .section h3 {
        margin: 8px 0 6px 0 !important;
        padding-bottom: 4px !important;
        border-bottom: 1px solid #000 !important;
        font-size: 13px;
    }

    .clinical-item {
        margin-bottom: 6px !important;
    }

    .clinical-item strong {
        margin-bottom: 2px !important;
    }

    .history-box,
    .clinical-value,
    .consultation-card,
    .clinical-record {
        background: white !important;
        box-shadow: none !important;
    }

    .history-box,
    .clinical-value {
        padding: 5px !important;
    }

    .consultation-card,
    .clinical-record {
        border: 1px solid #bbb !important;
        padding: 7px !important;
        margin-bottom: 7px !important;
        page-break-inside: avoid !important;
    }

    .consultation-title,
    .clinical-record-header {
        margin-bottom: 6px !important;
    }

    table {
        width: 100% !important;
        font-size: 10px !important;
    }

    th,
    td {
        padding: 4px !important;
    }

    #appointmentHistory button,
    .consultation-actions,
    .record-actions {
        display: none !important;
    }

    #message {
        display: none !important;
    }

    /* Keep related clinical sections together when possible */
    #appointmentHistory,
    #consultationHistory,
    #prescriptionHistory {
        page-break-inside: auto !important;
    }

    .record-actions,
.consultation-actions {
    display: none !important;
}

.clinical-grid {
    display: flex !important;
    flex-direction: column !important;
    gap: 6px !important;
}

.clinical-grid .clinical-item {
    width: 100% !important;
}

.clinical-item {
    page-break-inside: avoid !important;
}

.clinical-value {
    padding: 4px !important;
    border: 1px solid #ddd !important;
}
}


/* Patient Timeline - heading/date spacing */
#patientTimeline .timeline-type {
    margin-bottom: 10px !important;
}

#patientTimeline .timeline-meta {
    margin-top: 0 !important;
    line-height: 1.5;
}
/* ==========================================
   PATIENT TIMELINE
========================================== */

#patientTimeline {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.timeline-record {
    background: white;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 15px;
    text-align: left;
}

.timeline-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 15px;
    margin-bottom: 10px;
}

.timeline-type {
    font-weight: bold;
    font-size: 15px;
    margin-bottom: 7px;
}

.timeline-meta {
    color: #666;
    font-size: 13px;
    margin-top: 3px;
}

.timeline-title {
    margin-top: 8px;
    padding: 10px;
    background: #f8f9fa;
    border-radius: 5px;
    text-align: left;
}

.timeline-status {
    margin-top: 10px;
    font-weight: bold;
    text-align: left;
}

.timeline-status span {
    font-weight: normal;
}

.clinical-grid {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.clinical-grid .clinical-item {
    width: 100%;
    margin-bottom: 0;
}

.clinical-grid .clinical-item {
    margin-bottom: 0;
}


@media (max-width: 700px) {
    .clinical-grid {
        grid-template-columns: 1fr;
    }

    .clinical-grid .clinical-item:last-child {
        grid-column: auto;
    }
}

/* ==========================================
   PRESCRIPTION MEDICATION TABLE
========================================== */

.prescription-table-wrapper {
    width: 100%;
    overflow-x: auto;
    margin-top: 15px;
}

.prescription-entry-table {
    width: 100%;
    border-collapse: collapse;
    table-layout: fixed;
    background: white;
}

.prescription-entry-table th {
    background: #f0f3f5;
    border: 1px solid #ccc;
    padding: 10px;
    text-align: left;
    font-weight: bold;
    white-space: nowrap;
}

.prescription-entry-table td {
    border: 1px solid #ccc;
    padding: 8px;
    vertical-align: middle;
}

.prescription-entry-table input {
    width: 100%;
    padding: 9px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 14px;
    min-width: 0;
}

.prescription-entry-table th:nth-child(1),
.prescription-entry-table td:nth-child(1) {
    width: 22%;
}

.prescription-entry-table th:nth-child(2),
.prescription-entry-table td:nth-child(2) {
    width: 12%;
}

.prescription-entry-table th:nth-child(3),
.prescription-entry-table td:nth-child(3) {
    width: 12%;
}

.prescription-entry-table th:nth-child(4),
.prescription-entry-table td:nth-child(4) {
    width: 15%;
}

.prescription-entry-table th:nth-child(5),
.prescription-entry-table td:nth-child(5) {
    width: 14%;
}

.prescription-entry-table th:nth-child(6),
.prescription-entry-table td:nth-child(6) {
    width: 12%;
}

.prescription-entry-table th:nth-child(7),
.prescription-entry-table td:nth-child(7) {
    width: 13%;
}

.record-actions {
    display: flex;
    flex-direction: row;
    justify-content: flex-end;
    align-items: center;
    gap: 6px;
    margin-top: 12px;
    padding-top: 10px;
    border-top: 1px solid #eee;
}

.record-actions button {
    width: auto;
    padding: 6px 10px;
    font-size: 13px;
    line-height: 1.2;
}

.prescription-extra {
    margin-top: 18px;
}

/* =====================================================
   VITAL SIGNS TABLE
===================================================== */

#vitalSignsHistory {
    width: 100%;
}

#vitalSignsHistory h4 {
    font-size: 16px;
    font-weight: 600;
    letter-spacing: 0.2px;
    margin-bottom: 12px;
}

#vitalSignsHistory > div {
    width: 100%;
    overflow-x: auto;
}

#vitalSignsHistory table {
    width: 100%;
    min-width: 1050px;
    border-collapse: collapse;
    font-family: "Segoe UI", Arial, sans-serif;
    font-size: 13px;
}

#vitalSignsHistory th {
    white-space: nowrap;
    font-weight: 600;
    text-align: left;
    padding: 11px 12px;
}

#vitalSignsHistory td {
    white-space: nowrap;
    padding: 11px 12px;
    vertical-align: middle;
    font-weight: 400;
}

/* Keep values and units together */
#vitalSignsHistory td:nth-child(2),
#vitalSignsHistory td:nth-child(3),
#vitalSignsHistory td:nth-child(4),
#vitalSignsHistory td:nth-child(5),
#vitalSignsHistory td:nth-child(6),
#vitalSignsHistory td:nth-child(7),
#vitalSignsHistory td:nth-child(8),
#vitalSignsHistory td:nth-child(9) {
    white-space: nowrap;
}

/* Date and recorded-by columns */
#vitalSignsHistory td:first-child,
#vitalSignsHistory td:nth-child(10) {
    white-space: nowrap;
}

/* Delete button */
#vitalSignsHistory .danger-btn {
    white-space: nowrap;
    font-family: "Segoe UI", Arial, sans-serif;
}

#labRequestPrintable .signature-pad-wrapper {
    width: 100%;
    max-width: 500px;
    height: 160px;
    border: 1px solid #9ca3af;
    border-radius: 5px;
    background: #fff;
    margin-top: 8px;
    overflow: hidden;
}

#labRequestPrintable #labRequestSignaturePad {
    display: block;
    width: 100%;
    height: 100%;
    cursor: crosshair;
    background: #fff;
}

#labRequestPrintable .signature-actions {
    margin-top: 8px;
}

#labRequestPrintable .signature-actions button {
    font-size: 11px;
    padding: 6px 10px;
}

#labRequestPrintable .signature-status {
    margin-top: 5px;
    font-size: 10px;
    color: #666;
}

@media print {

    #labRequestPrintable .signature-pad-wrapper {
        border: none;
        height: auto;
    }

    #labRequestPrintable #labRequestSignaturePad {
        border: none;
        height: 90px;
    }

    #labRequestPrintable .signature-actions,
    #labRequestPrintable .signature-status {
        display: none !important;
    }

}

/* =====================================================
   LABORATORY REQUEST HISTORY
===================================================== */

.lab-request-card {
    background: #ffffff;
    border: 1px solid #e2e8f0;
    border-radius: 14px;
    margin-bottom: 20px;
    overflow: hidden;
    box-shadow: 0 3px 12px rgba(15, 23, 42, 0.06);
    transition: box-shadow 0.2s ease, transform 0.2s ease;
}

.lab-request-card:hover {
    box-shadow: 0 6px 20px rgba(15, 23, 42, 0.10);
    transform: translateY(-1px);
}


/* HEADER */

.lab-request-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
    padding: 18px 22px;
    background: #f8fafc;
    border-bottom: 1px solid #e2e8f0;
}

.lab-request-title {
    display: flex;
    align-items: center;
    gap: 13px;
}

.lab-request-icon {
    width: 42px;
    height: 42px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #e0f2fe;
    border-radius: 10px;
    font-size: 21px;
}

.lab-request-title h4 {
    margin: 0;
    font-size: 16px;
    font-weight: 700;
    color: #0f172a;
}

.lab-request-id {
    margin-top: 3px;
    font-size: 12px;
    color: #64748b;
}

.lab-request-date {
    text-align: right;
}

.lab-request-date-label {
    display: block;
    font-size: 11px;
    color: #64748b;
    margin-bottom: 3px;
}

.lab-request-date strong {
    font-size: 13px;
    color: #334155;
}


/* BODY */

.lab-request-card-body {
    padding: 22px;
}

.lab-request-field {
    margin-bottom: 18px;
}

.lab-request-field-label {
    display: flex;
    align-items: center;
    gap: 7px;
    margin-bottom: 8px;
    font-size: 12px;
    font-weight: 700;
    color: #475569;
    text-transform: uppercase;
    letter-spacing: 0.03em;
}

.field-icon {
    font-size: 14px;
}

.lab-request-field-value {
    font-size: 14px;
    color: #1e293b;
    line-height: 1.6;
}


/* TEST BADGES */

.lab-request-tests {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
}

.lab-test-badge {
    display: inline-flex;
    align-items: center;
    padding: 7px 11px;
    background: #eff6ff;
    border: 1px solid #bfdbfe;
    color: #1d4ed8;
    border-radius: 7px;
    font-size: 13px;
    font-weight: 600;
}


/* DETAILS GRID */

.lab-request-details-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 18px;
    padding-top: 4px;
}

.lab-request-details-grid .lab-request-field {
    margin-bottom: 0;
    padding: 14px;
    background: #f8fafc;
    border: 1px solid #e2e8f0;
    border-radius: 9px;
}

.lab-request-clinical-info {
    grid-column: 1 / -1;
}


/* MUTED TEXT */

.lab-request-muted {
    color: #94a3b8;
    font-style: italic;
}


/* SIGNATURE */

.lab-request-signature-section {
    margin-top: 20px;
    padding-top: 18px;
    border-top: 1px solid #e2e8f0;
}

.lab-request-signature {
    display: inline-flex;
    flex-direction: column;
    align-items: flex-start;
    padding: 4px 8px;
    background: #ffffff;
    border: 1px solid #dbeafe;
    border-radius: 9px;
}

.lab-request-signature img {
    display: block;
    width: auto;
    max-width: 300px;
    height: auto;
    max-height: 100px;
    object-fit: contain;
}

.lab-request-signature-status {
    margin-top: 3px;
    font-size: 11px;
    color: #15803d;
    font-weight: 700;
}

.lab-request-no-signature {
    display: inline-flex;
    padding: 10px 13px;
    background: #f8fafc;
    border: 1px dashed #cbd5e1;
    border-radius: 8px;
    color: #94a3b8;
    font-size: 13px;
}


/* FOOTER */

.lab-request-card-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 15px;
    padding: 14px 22px;
    background: #f8fafc;
    border-top: 1px solid #e2e8f0;
}

.lab-request-created {
    font-size: 11px;
    color: #94a3b8;
}

.lab-request-actions {
    display: flex;
    gap: 8px;
}

.lab-request-print-btn,
.lab-request-delete-btn {
    border: none;
    border-radius: 7px;
    padding: 8px 13px;
    font-size: 12px;
    font-weight: 600;
    cursor: pointer;
}

.lab-request-print-btn {
    background: #2563eb;
    color: #ffffff;
}

.lab-request-print-btn:hover {
    background: #1d4ed8;
}

.lab-request-delete-btn {
    background: #fee2e2;
    color: #b91c1c;
}

.lab-request-delete-btn:hover {
    background: #fecaca;
}


/* LOADING */

.lab-request-loading {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
    padding: 35px;
    color: #64748b;
    font-size: 14px;
}

.loading-spinner {
    width: 18px;
    height: 18px;
    border: 2px solid #dbeafe;
    border-top-color: #2563eb;
    border-radius: 50%;
    animation: labRequestSpin 0.8s linear infinite;
}

@keyframes labRequestSpin {
    to {
        transform: rotate(360deg);
    }
}


/* EMPTY */

.lab-request-empty {
    text-align: center;
    padding: 45px 20px;
    border: 1px dashed #cbd5e1;
    border-radius: 12px;
    background: #f8fafc;
}

.lab-request-empty-icon {
    font-size: 32px;
    margin-bottom: 10px;
}

.lab-request-empty h4 {
    margin: 0 0 6px;
    color: #334155;
}

.lab-request-empty p {
    margin: 0;
    color: #94a3b8;
    font-size: 13px;
}


/* ERROR */

.lab-request-error {
    display: flex;
    flex-direction: column;
    gap: 5px;
    padding: 18px;
    background: #fef2f2;
    border: 1px solid #fecaca;
    border-radius: 9px;
    color: #991b1b;
}

.lab-request-error span {
    font-size: 13px;
}


/* MOBILE */

@media (max-width: 700px) {

    .lab-request-card-header {
        align-items: flex-start;
        flex-direction: column;
    }

    .lab-request-date {
        text-align: left;
    }

    .lab-request-details-grid {
        grid-template-columns: 1fr;
    }

    .lab-request-clinical-info {
        grid-column: auto;
    }

    .lab-request-card-footer {
        align-items: flex-start;
        flex-direction: column;
    }

    .lab-request-actions {
        width: 100%;
    }

    .lab-request-print-btn,
    .lab-request-delete-btn {
        flex: 1;
    }
}


/* ==========================================
   PATIENT PROFILE CLINICAL DEPARTMENTS
   ========================================== */

.patient-profile-modal .clinical-department {
    margin: 8px 0;
    border: 1px solid #d7dee7;
    border-radius: 8px;
    overflow: hidden;
    background: #ffffff;
}

.patient-profile-modal .clinical-department-header {
    padding: 8px 14px;
    border-left: 4px solid;
}

.patient-profile-modal .clinical-department-header h4 {
    margin: 0;
    font-size: 15px;
    font-weight: 600;
}

/* Vital Signs */
.patient-profile-modal .clinical-department-vitals {
    border-color: #80cbc4;
    border-top: 3px solid #00897b;
}

.patient-profile-modal .clinical-department-header-vitals {
    background: #e8f5f3;
    border-left-color: #00897b;
}

.patient-profile-modal .clinical-department-header-vitals h4 {
    color: #00695c;
}

/* Laboratory Requests */
.patient-profile-modal .clinical-department-lab-requests {
    border-color: #b39ddb;
    border-top: 3px solid #7e57c2;
}

.patient-profile-modal .clinical-department-header-lab-requests {
    background: #f1ecf8;
    border-left-color: #7e57c2;
}

.patient-profile-modal .clinical-department-header-lab-requests h4 {
    color: #5e35b1;
}

/* Laboratory Results */
.patient-profile-modal .clinical-department-lab-results {
    border-color: #90caf9;
    border-top: 3px solid #1e88e5;
}

.patient-profile-modal .clinical-department-header-lab-results {
    background: #eaf3fc;
    border-left-color: #1e88e5;
}

.patient-profile-modal .clinical-department-header-lab-results h4 {
    color: #1565c0;
}

/* Laboratory Reports */
.patient-profile-modal .clinical-department-lab-reports {
    border-color: #9fa8da;
    border-top: 3px solid #3949ab;
}

.patient-profile-modal .clinical-department-header-lab-reports {
    background: #eef0fb;
    border-left-color: #3949ab;
}

.patient-profile-modal .clinical-department-header-lab-reports h4 {
    color: #303f9f;
}

/* Prescriptions */
/* Space between consultation clinical departments */
.patient-profile-modal .consultation-linked-records {
    display: flex;
    flex-direction: column;
    gap: 16px;
}
/* Visible column dividers for consultation department tables */
.patient-profile-modal .consultation-linked-records table th,
.patient-profile-modal .consultation-linked-records table td {
    border-right: 1px solid #d0d0d0;
}

.patient-profile-modal .consultation-linked-records table th:last-child,
.patient-profile-modal .consultation-linked-records table td:last-child {
    border-right: none;
}
.patient-profile-modal .clinical-department-prescriptions {
    border-color: #81c784;
    border-top: 3px solid #43a047;
}

.patient-profile-modal .clinical-department-header-prescriptions {
    background: #edf7ee;
    border-left-color: #43a047;
}

.patient-profile-modal .clinical-department-header-prescriptions h4 {
    color: #2e7d32;
}

/* Tight spacing between departments */
.patient-profile-modal .clinical-department + .clinical-department {
    margin-top: 8px;
}

.patient-profile-modal .clinical-department table {
    margin-top: 4px;
}

/* ==========================================
   PRESCRIPTION MEDICATION TABLE
========================================== */

.clinical-prescription-table {
    width: 100% !important;
    border-collapse: collapse !important;
    table-layout: fixed !important;
    margin-top: 8px !important;
    background: #ffffff !important;
}

.clinical-prescription-table th {
    padding: 9px 8px !important;
    text-align: center !important;
    vertical-align: middle !important;
    background: #f1f8f2 !important;
    color: #4d7c50 !important;
    border: 1px solid #a5d6a7 !important;
    font-size: 12px !important;
    font-weight: 700 !important;
    line-height: 1.3 !important;
}

.clinical-prescription-table td {
    padding: 10px 8px !important;
    text-align: center !important;
    vertical-align: middle !important;
    border: 1px solid #d9e2e7 !important;
    color: #263238 !important;
    font-size: 13px !important;
    font-weight: 500 !important;
    line-height: 1.4 !important;
}

.clinical-prescription-table tbody tr:hover {
    background: #fafdfb !important;
}

.prescription-items h4 {
    margin: 10px 0 6px 0 !important;
    color: #4d7c50 !important;
    font-size: 14px !important;
    font-weight: 700 !important;
}


</style>

<style>

/* =====================================================
   FRANTETT LABORATORY REQUEST FORM
===================================================== */

#labRequestPrintable {
    background: #ffffff;
    color: #1f2937;
    font-family: Arial, Helvetica, sans-serif;
    max-width: 820px;
    margin: 0 auto;
    padding: 28px 32px;
}

#labRequestPrintable .lab-request-header {
    text-align: center;
    border-bottom: 3px solid #1f4e79;
    padding-bottom: 18px;
    margin-bottom: 22px;
}

#labRequestPrintable .lab-request-header h1 {
    margin: 0;
    font-size: 30px;
    font-weight: 700;
    letter-spacing: 1px;
    color: #1f4e79;
}

#labRequestPrintable .lab-request-header p {
    margin: 5px 0 12px;
    font-size: 13px;
    color: #555;
}

#labRequestPrintable .lab-request-header h2 {
    margin: 0;
    font-size: 20px;
    letter-spacing: .5px;
    color: #222;
}

#labRequestPrintable .lab-request-patient-section,
#labRequestPrintable .lab-request-section {
    margin-bottom: 20px;
}

#labRequestPrintable h3 {
    margin: 0 0 10px;
    padding: 8px 10px;
    background: #f1f5f9;
    border-left: 4px solid #1f4e79;
    font-size: 14px;
    color: #1f2937;
}

#labRequestPrintable .lab-request-patient-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    border: 1px solid #d1d5db;
    border-radius: 4px;
    overflow: hidden;
}

#labRequestPrintable .lab-request-patient-grid > div {
    padding: 10px 12px;
    border-bottom: 1px solid #e5e7eb;
    font-size: 12px;
}

#labRequestPrintable .lab-request-patient-grid > div:nth-child(odd) {
    border-right: 1px solid #e5e7eb;
}

#labRequestPrintable .lab-request-patient-grid strong {
    display: inline-block;
    margin-right: 5px;
}

#labRequestPrintable .lab-request-tests {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 9px 22px;
    border: 1px solid #d1d5db;
    padding: 14px;
    border-radius: 4px;
}

#labRequestPrintable .lab-request-tests label {
    font-size: 12px;
    display: flex;
    align-items: center;
    gap: 7px;
}

#labRequestPrintable .lab-request-tests input {
    width: 14px;
    height: 14px;
}

#labRequestPrintable .form-group {
    margin-top: 12px;
}

#labRequestPrintable .form-group label {
    display: block;
    font-size: 12px;
    font-weight: 600;
    margin-bottom: 5px;
}

#labRequestPrintable input[type="text"],
#labRequestPrintable textarea {
    width: 100%;
    border: 1px solid #cbd5e1;
    border-radius: 4px;
    padding: 9px 10px;
    font-family: inherit;
    font-size: 12px;
    background: #fff;
}

#labRequestPrintable textarea {
    min-height: 85px;
    resize: vertical;
}

#labRequestPrintable .lab-request-signature {
    display: grid;
    grid-template-columns: 1fr 180px;
    gap: 35px;
    margin-top: 32px;
    font-size: 12px;
}

#labRequestPrintable .signature-line {
    border-bottom: 1px solid #444;
    height: 28px;
    margin-top: 8px;
}

#labRequestPrintable .lab-request-footer {
    margin-top: 38px;
    padding-top: 10px;
    border-top: 1px solid #cbd5e1;
    text-align: center;
    font-size: 8px;
    line-height: 1.45;
    color: #555;
}

#labRequestPrintable .lab-request-footer strong {
    display: block;
    font-size: 9px;
    color: #333;
    margin-bottom: 2px;
}

@media print {

    #labRequestPrintable {
        max-width: none;
        padding: 0;
    }

    #labRequestPrintable .no-print {
        display: none !important;
    }

    #labRequestPrintable input,
    #labRequestPrintable textarea {
        border: none;
        padding: 0;
        background: transparent;
    }

    #labRequestPrintable textarea {
        min-height: 70px;
    }

    #labRequestPrintable .lab-request-footer {
        font-size: 8px;
    }

}

/* ============================================================
   CONSULTATION DEPARTMENT COLORS
============================================================ */

#completeConsultationClinicalRecord {
    width: 100% !important;
}

#completeConsultationClinicalRecord .consultation-clinical-section {
    margin-top: 20px !important;
    margin-bottom: 20px !important;
    border-radius: 8px !important;
    overflow: hidden !important;
    background: #ffffff !important;
}

/* Vital Signs */
#completeConsultationClinicalRecord .clinical-vital-signs,
.modal-content .clinical-vital-signs {
    border: 1px solid #80cbc4 !important;
    border-top: 4px solid #00897b !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs > .top-actions {
    background: #e8f5f3 !important;
    border-left: 4px solid #00897b !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs h3 {
    color: #00695c !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .consultation-clinical-grid,
.modal-content .clinical-vital-signs .consultation-clinical-grid {
    display: grid !important;
    grid-template-columns: repeat(10, minmax(0, 1fr)) !important;
    gap: 0 !important;
    width: 100% !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .consultation-clinical-grid,
.modal-content .clinical-vital-signs .consultation-clinical-grid {
    display: grid !important;
    grid-template-columns: repeat(10, minmax(0, 1fr)) !important;
    grid-template-rows: auto auto !important;
    gap: 0 !important;
    width: 100% !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .consultation-clinical-item,
.modal-content .clinical-vital-signs .consultation-clinical-item {
    display: contents !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .consultation-clinical-item strong,
.modal-content .clinical-vital-signs .consultation-clinical-item strong {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    min-width: 0 !important;
    padding: 10px 6px !important;
    text-align: center !important;
    font-weight: 700 !important;
    background: #f4f7f8 !important;
    border: 1px solid #d9e2e7 !important;
    white-space: normal !important;
    line-height: 1.2 !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .consultation-clinical-item > div,
.modal-content .clinical-vital-signs .consultation-clinical-item > div {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    min-width: 0 !important;
    padding: 10px 6px !important;
    text-align: center !important;
    border: 1px solid #d9e2e7 !important;
    border-top: 0 !important;
    white-space: nowrap !important;
    min-height: 38px !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .consultation-clinical-item strong,
.modal-content .clinical-vital-signs .consultation-clinical-item strong {
    display: block !important;
    margin-bottom: 6px !important;
    font-weight: 700 !important;
    white-space: normal !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .consultation-clinical-item > div,
.modal-content .clinical-vital-signs .consultation-clinical-item > div {
    white-space: nowrap !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .clinical-table,
.modal-content .clinical-vital-signs .clinical-table {
    width: 100% !important;
    table-layout: fixed !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .clinical-table th,
#completeConsultationClinicalRecord .clinical-vital-signs .clinical-table td,
.modal-content .clinical-vital-signs .clinical-table th,
.modal-content .clinical-vital-signs .clinical-table td {
    text-align: center !important;
    vertical-align: middle !important;
    padding: 10px 8px !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .clinical-table th,
.modal-content .clinical-vital-signs .clinical-table th {
    white-space: normal !important;
    font-weight: 700 !important;
}

#completeConsultationClinicalRecord .clinical-vital-signs .clinical-table td,
.modal-content .clinical-vital-signs .clinical-table td {
    white-space: nowrap !important;
}

/* Laboratory Requests */
#completeConsultationClinicalRecord .clinical-laboratory-requests {
    border: 1px solid #b39ddb !important;
    border-top: 4px solid #7e57c2 !important;
}

#completeConsultationClinicalRecord .clinical-laboratory-requests > .top-actions {
    background: #f1ecf8 !important;
    border-left: 4px solid #7e57c2 !important;
}

#completeConsultationClinicalRecord .clinical-laboratory-requests h3 {
    color: #5e35b1 !important;
}

/* Laboratory Results */
#completeConsultationClinicalRecord .clinical-laboratory-results {
    border: 1px solid #90caf9 !important;
    border-top: 4px solid #1e88e5 !important;
}

#completeConsultationClinicalRecord .clinical-laboratory-results > .top-actions {
    background: #eaf3fc !important;
    border-left: 4px solid #1e88e5 !important;
}

#completeConsultationClinicalRecord .clinical-laboratory-results h3 {
    color: #1565c0 !important;
}

/* Laboratory Reports */
#completeConsultationClinicalRecord .clinical-laboratory-reports {
    border: 1px solid #9fa8da !important;
    border-top: 4px solid #3949ab !important;
}

#completeConsultationClinicalRecord .clinical-laboratory-reports > .top-actions {
    background: #eef0fb !important;
    border-left: 4px solid #3949ab !important;
}

#completeConsultationClinicalRecord .clinical-laboratory-reports h3 {
    color: #303f9f !important;
}

/* Prescriptions */
#completeConsultationClinicalRecord .clinical-prescriptions {
    border: 1px solid #81c784 !important;
    border-top: 4px solid #43a047 !important;
}

#completeConsultationClinicalRecord .clinical-prescriptions > .top-actions {
    background: #edf7ee !important;
    border-left: 4px solid #43a047 !important;
}

#completeConsultationClinicalRecord .clinical-prescriptions h3 {
    color: #2e7d32 !important;
}

/* Content spacing */
#completeConsultationClinicalRecord .consultation-clinical-content {
    padding: 14px 15px !important;
}

/* ============================================================
   END CONSULTATION DEPARTMENT COLORS
============================================================ */

.consultation-clinical-section {
    margin-top: 24px;
}

.consultation-clinical-content {
    padding: 10px 0;
}

.consultation-clinical-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
}

.consultation-clinical-item {
    padding: 14px;
    border: 1px solid #ddd;
    border-radius: 8px;
    background: #fff;
}

.consultation-clinical-item strong {
    display: block;
    margin-bottom: 6px;
}

.clinical-empty {
    padding: 15px;
    border-radius: 8px;
    background: #f5f5f5;
    font-style: italic;
}

.prescription-items {
    margin-top: 20px;
}

.prescription-items h4 {
    margin-bottom: 12px;
}

@media (max-width: 700px) {

    .consultation-clinical-grid {
        grid-template-columns: 1fr;
    }

}


/* ==========================================
   CONSULTATION DETAILS - EMR DISPLAY
========================================== */

.clinical-details-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
    margin-top: 14px;
}

.clinical-detail-item {
    min-width: 0;
    padding: 0 0 10px 0 !important;
    border: none !important;
    border-radius: 0 !important;
    background: transparent !important;
}

.clinical-detail-wide {
    grid-column: 1 / -1;
}

.clinical-detail-label {
    display: block !important;
    width: 100% !important;
    box-sizing: border-box !important;
    margin: 0 0 5px 0 !important;
    padding: 7px 11px !important;
    background: #f1f8f2 !important;
    border-left: 4px solid #a5d6a7 !important;
    border-bottom: 1px solid #d7ead8 !important;
    border-radius: 4px !important;
    color: #4d7c50 !important;
    font-size: 13px !important;
    font-weight: 700 !important;
    line-height: 1.35 !important;
    text-transform: none !important;
    letter-spacing: normal !important;
}

.clinical-detail-value {
    display: block !important;
    min-height: 20px !important;
    padding: 3px 11px 5px 11px !important;
    color: #263238 !important;
    background: transparent !important;
    font-size: 14px !important;
    font-weight: 400 !important;
    line-height: 1.55 !important;
    white-space: pre-wrap !important;
    overflow-wrap: anywhere !important;
}

@media (max-width: 700px) {
    .clinical-details-grid {
        grid-template-columns: 1fr;
    }

    .clinical-detail-wide {
        grid-column: auto;
    }
}

/* Clean horizontal Vital Signs display */
.clinical-vitals-horizontal {
    width: 100% !important;
    margin: 8px 0 12px 0 !important;
}

.clinical-vitals-table {
    width: 100% !important;
    border-collapse: collapse !important;
    table-layout: fixed !important;
    background: #ffffff !important;
}

.clinical-vitals-table th {
    padding: 9px 7px !important;
    text-align: center !important;
    vertical-align: middle !important;
    background: #f1f8f2 !important;
    color: #4d7c50 !important;
    border: 1px solid #d7ead8 !important;
    font-weight: 700 !important;
    font-size: 12px !important;
    line-height: 1.2 !important;
}

.clinical-vitals-table td {
    padding: 10px 7px !important;
    text-align: center !important;
    vertical-align: middle !important;
    border: 1px solid #e1e8e2 !important;
    font-size: 13px !important;
    font-weight: 600 !important;
    white-space: nowrap !important;
}

.clinical-vitals-recorded-by,
.clinical-vitals-recorded-at {
    display: inline-block !important;
    margin-top: 8px !important;
    margin-right: 20px !important;
    font-size: 13px !important;
}

.clinical-vitals-recorded-by strong,
.clinical-vitals-recorded-at strong {
    color: #4d7c50 !important;
}

/* View Consultation - soft green clinical headings */
#viewConsultationModal .clinical-item {
    margin-bottom: 10px !important;
}

#viewConsultationModal .clinical-item > strong {
    display: block !important;
    width: 100% !important;
    box-sizing: border-box !important;
    margin: 0 0 5px 0 !important;
    padding: 7px 11px !important;
    background: #f1f8f2 !important;
    border-left: 4px solid #a5d6a7 !important;
    border-bottom: 1px solid #d7ead8 !important;
    border-radius: 4px !important;
    color: #4d7c50 !important;
    font-size: 13px !important;
    font-weight: 700 !important;
    line-height: 1.35 !important;
}

#viewConsultationModal .clinical-item .clinical-value {
    display: block !important;
    padding: 3px 11px 7px 11px !important;
    margin: 0 !important;
    color: #263238 !important;
    background: transparent !important;
    font-size: 14px !important;
    font-weight: 400 !important;
    line-height: 1.55 !important;
    white-space: pre-wrap !important;
}

</style>

<style id="frantett-professional-consultation-edit-styles">

/* =====================================================
   FRANTETT PROFESSIONAL CONSULTATION EDIT STYLES
   Scoped to Edit Consultation mode only.
   Existing clinical structure is preserved.
===================================================== */

#consultationFormModal.unified-consultation-edit-mode
.modal-content {

    background:
        linear-gradient(
            180deg,
            #ffffff 0%,
            #fbfdfc 100%
        );

    border:
        1px solid #d7e5df;

    box-shadow:
        0 24px 60px rgba(18, 53, 91, 0.18);

}


/* -----------------------------------------------------
   EDIT TITLE
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
#consultationFormTitle {

    display:
        flex;

    align-items:
        center;

    gap:
        12px;

    color:
        #12355B;

    font-weight:
        750;

    letter-spacing:
        -0.2px;

    padding-bottom:
        14px;

    border-bottom:
        1px solid #e2ece8;

}


/* -----------------------------------------------------
   EDIT MODE BADGE
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
#consultationFormTitle::after {

    content:
        "EDITING CLINICAL RECORDS";

    display:
        inline-flex;

    align-items:
        center;

    min-height:
        28px;

    padding:
        0 11px;

    border-radius:
        999px;

    background:
        #eaf6f1;

    border:
        1px solid #c9e4d9;

    color:
        #0b6b5c;

    font-size:
        11px;

    font-weight:
        800;

    letter-spacing:
        0.5px;

}


/* -----------------------------------------------------
   EXISTING SECTION HEADERS
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.section {

    border:
        1px solid #e1ebe7;

    border-radius:
        12px;

    background:
        #ffffff;

    box-shadow:
        0 5px 18px rgba(18, 53, 91, 0.05);

    margin-bottom:
        20px;

}


#consultationFormModal.unified-consultation-edit-mode
.section > .top-actions {

    background:
        linear-gradient(
            90deg,
            #f1f8f5,
            #f8fbfa
        );

    border-bottom:
        1px solid #deebe6;

    border-left:
        4px solid #0b6b5c;

    border-radius:
        11px 11px 0 0;

    padding:
        13px 15px;

}


#consultationFormModal.unified-consultation-edit-mode
.section > .top-actions h3 {

    color:
        #12355B;

    font-size:
        16px;

    font-weight:
        750;

    margin:
        0;

}


/* -----------------------------------------------------
   EXISTING CLINICAL TABLES
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
table.clinical-table {

    width:
        100%;

    border-collapse:
        separate;

    border-spacing:
        0;

    overflow:
        hidden;

    background:
        #ffffff;

    border:
        1px solid #dce8e4;

    border-radius:
        9px;

    box-shadow:
        0 2px 8px rgba(18, 53, 91, 0.04);

}


#consultationFormModal.unified-consultation-edit-mode
table.clinical-table thead th {

    background:
        #f3f7f6;

    color:
        #24443d;

    font-size:
        12px;

    font-weight:
        800;

    letter-spacing:
        0.2px;

    text-align:
        left;

    padding:
        11px 10px;

    border-bottom:
        1px solid #dce8e4;

    white-space:
        nowrap;

}


#consultationFormModal.unified-consultation-edit-mode
table.clinical-table tbody td {

    padding:
        10px;

    color:
        #34433f;

    background:
        #ffffff;

    border-bottom:
        1px solid #edf2f0;

    vertical-align:
        middle;

}


#consultationFormModal.unified-consultation-edit-mode
table.clinical-table tbody tr:last-child td {

    border-bottom:
        none;

}


#consultationFormModal.unified-consultation-edit-mode
table.clinical-table tbody tr:hover td {

    background:
        #f9fcfb;

}


/* -----------------------------------------------------
   INLINE EDIT FIELDS
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.consultation-inline-edit,
#consultationFormModal.unified-consultation-edit-mode
.unified-inline-edit {

    display:
        block;

    width:
        100%;

    min-height:
        38px;

    box-sizing:
        border-box;

    padding:
        8px 10px;

    border:
        1px solid #c7d8d2;

    border-radius:
        7px;

    background:
        #ffffff;

    color:
        #243630;

    font:
        inherit;

    outline:
        none;

    transition:
        border-color 0.18s ease,
        box-shadow 0.18s ease,
        background 0.18s ease;

}


#consultationFormModal.unified-consultation-edit-mode
.consultation-inline-edit:hover,
#consultationFormModal.unified-consultation-edit-mode
.unified-inline-edit:hover {

    border-color:
        #9dbcb0;

}


#consultationFormModal.unified-consultation-edit-mode
.consultation-inline-edit:focus,
#consultationFormModal.unified-consultation-edit-mode
.unified-inline-edit:focus {

    border-color:
        #0b6b5c;

    background:
        #fcfffd;

    box-shadow:
        0 0 0 3px rgba(11, 107, 92, 0.10);

}


#consultationFormModal.unified-consultation-edit-mode
textarea.consultation-inline-edit,
#consultationFormModal.unified-consultation-edit-mode
textarea.unified-inline-edit {

    min-height:
        72px;

    line-height:
        1.45;

}


/* -----------------------------------------------------
   LABORATORY REPORT CARDS
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report {

    margin:
        14px 0;

    padding:
        15px;

    border:
        1px solid #dce6f1;

    border-left:
        4px solid #3949ab;

    border-radius:
        10px;

    background:
        #fbfcff;

    box-shadow:
        0 3px 12px rgba(48, 63, 159, 0.05);

}


#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report .record-meta {

    color:
        #4c5c68;

    line-height:
        1.7;

}


#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report
.clinical-lab-report-title {

    color:
        #303f9f;

    font-size:
        15px;

    font-weight:
        750;

}


/* -----------------------------------------------------
   PRESCRIPTION CARDS
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-prescription {

    margin:
        14px 0;

    padding:
        15px;

    border:
        1px solid #dce9dd;

    border-left:
        4px solid #43a047;

    border-radius:
        10px;

    background:
        #fcfffc;

    box-shadow:
        0 3px 12px rgba(67, 160, 71, 0.05);

}


#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-prescription .record-meta {

    color:
        #415146;

    line-height:
        1.7;

}


/* -----------------------------------------------------
   VITALS
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-vital-table {

    min-width:
        900px;

}


#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-vital-table
tbody tr td {

    min-width:
        105px;

}


/* -----------------------------------------------------
   LAB REQUEST TABLE
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-request-table {

    min-width:
        760px;

}


#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-request-table
tbody td {

    vertical-align:
        top;

}


/* -----------------------------------------------------
   LAB REPORT PARAMETER TABLE
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report
table.clinical-table {

    margin-top:
        12px;

}


#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report
table.clinical-table thead th {

    background:
        #eef0fb;

    color:
        #303f9f;

}


/* -----------------------------------------------------
   PRESCRIPTION TABLE
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-prescription
table.clinical-table thead th {

    background:
        #edf7ee;

    color:
        #2e7d32;

}


/* -----------------------------------------------------
   RECORD META
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.record-meta {

    margin-top:
        8px;

    color:
        #50605a;

    font-size:
        13px;

}


/* -----------------------------------------------------
   EDIT MODE ACTION BAR
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.form-actions {

    position:
        sticky;

    bottom:
        0;

    z-index:
        20;

    margin-top:
        22px;

    padding:
        14px 0 4px;

    background:
        linear-gradient(
            180deg,
            rgba(255,255,255,0),
            #ffffff 22%
        );

}


#consultationFormModal.unified-consultation-edit-mode
.form-actions .save-btn {

    min-width:
        170px;

    border-radius:
        8px;

    font-weight:
        750;

    box-shadow:
        0 5px 14px rgba(11, 107, 92, 0.14);

}


#consultationFormModal.unified-consultation-edit-mode
.form-actions .cancel-btn {

    min-width:
        110px;

    border-radius:
        8px;

}


/* -----------------------------------------------------
   SCROLLING TABLE WRAPPERS
----------------------------------------------------- */

#consultationFormModal.unified-consultation-edit-mode
.clinical-table-wrapper {

    overflow-x:
        auto;

    scrollbar-width:
        thin;

}


/* -----------------------------------------------------
   SMALL SCREEN
----------------------------------------------------- */

@media (max-width: 768px) {

    #consultationFormModal.unified-consultation-edit-mode
    #consultationFormTitle {

        flex-wrap:
            wrap;

    }

    #consultationFormModal.unified-consultation-edit-mode
    #consultationFormTitle::after {

        font-size:
            10px;

    }

    #consultationFormModal.unified-consultation-edit-mode
    .section > .top-actions {

        padding:
            11px 12px;

    }

    #consultationFormModal.unified-consultation-edit-mode
    table.clinical-table {

        font-size:
            12px;

    }

}


/* =====================================================
   FRANTETT HORIZONTAL LAB REPORT META
===================================================== */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report > .record-meta {

    display: flex;

    align-items: center;

    flex-wrap: nowrap;

    gap: 7px;

    white-space: nowrap;

    overflow-x: auto;

    padding: 4px 0 7px;

}


/* Keep labels and separators on the same line */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report > .record-meta strong {

    flex: 0 0 auto;

}


/* Keep controls compact and inline */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report > .record-meta
.consultation-inline-edit,
#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report > .record-meta
.unified-inline-edit {

    display: inline-block;

    width: auto;

    min-width: 0;

    max-width: none;

    min-height: 34px;

    flex: 0 0 auto;

    margin: 0;

}


/* Date */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report > .record-meta
input[type="date"] {

    width: 145px;

    min-width: 145px;

}


/* Status */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report > .record-meta
select {

    width: 135px;

    min-width: 135px;

}


/* Recorded By */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report > .record-meta
input[data-field="recorded_by"] {

    width: 180px;

    min-width: 180px;

}


/* Preserve one horizontal row */

#consultationFormModal.unified-consultation-edit-mode
.consultation-edit-lab-report > .record-meta {

    scrollbar-width: thin;

}


/* On smaller screens scroll horizontally instead of wrapping */

@media (max-width: 900px) {

    #consultationFormModal.unified-consultation-edit-mode
    .consultation-edit-lab-report > .record-meta {

        overflow-x: auto;

        flex-wrap: nowrap;

    }

}


/* =====================================================
   END FRANTETT HORIZONTAL LAB REPORT META
===================================================== */

/* =====================================================
   END PROFESSIONAL CONSULTATION EDIT STYLES
===================================================== */

</style>

<style>
/* =========================================================
   STAGE 5 � MOBILE CLINICAL TABLE SCROLL FIX
   Scope: Vital Signs, Prescriptions, Lab Requests,
          Lab Results
   ========================================================= */


/* ---------------------------------------------------------
   COMMON MOBILE TABLE SAFETY
   --------------------------------------------------------- */

@media (max-width: 700px) {

    /*
       Keep horizontal scrolling INSIDE the clinical table
       instead of allowing the whole page to move sideways.
    */

    #vitalSignsHistory,
    .prescription-items,
    #labRequestsHistory,
    .lab-table-wrapper,
    .clinical-lab-report-table-wrapper {

        max-width: 100%;
        min-width: 0;
    }


    /*
       Mobile scroll prompt
    */

    .clinical-mobile-scroll-prompt {

        display: block;
        width: 100%;
        box-sizing: border-box;

        margin-top: 6px;
        padding: 6px 8px;

        text-align: center;

        font-size: 11px;
        line-height: 1.3;

        color: #555;
        background: #f5f5f5;

        border-radius: 5px;
    }
}


/* ---------------------------------------------------------
   VITAL SIGNS
   --------------------------------------------------------- */

.clinical-vitals-horizontal {

    max-width: 100%;
    min-width: 0;
}


.clinical-vitals-horizontal .clinical-vitals-table {

    width: 100%;
    min-width: 850px;

    border-collapse: collapse;
}


@media (max-width: 700px) {

    .clinical-vitals-horizontal {

        width: 100%;
        min-width: 0;

        overflow: hidden;
    }


    .clinical-vitals-horizontal .clinical-vitals-table {

        display: block;

        width: 100%;
        max-width: 100%;

        min-width: 850px;

        overflow-x: auto;
        overflow-y: hidden;

        -webkit-overflow-scrolling: touch;

        box-sizing: border-box;
    }


    .clinical-vitals-horizontal .clinical-vitals-table th,
    .clinical-vitals-horizontal .clinical-vitals-table td {

        padding: 7px 8px;
        font-size: 12px;
        line-height: 1.3;

        white-space: nowrap;
    }


    .clinical-vitals-horizontal::after {

        content: "? Swipe or scroll right to see more";

        display: block;

        width: 100%;
        box-sizing: border-box;

        margin-top: 6px;
        padding: 6px 8px;

        text-align: center;

        font-size: 11px;
        line-height: 1.3;

        color: #555;
        background: #f5f5f5;

        border-radius: 5px;
    }


    .clinical-vitals-recorded-by,
    .clinical-vitals-recorded-at {

        font-size: 12px;
        line-height: 1.4;

        overflow-wrap: anywhere;
        word-break: break-word;
    }
}


/* ---------------------------------------------------------
   PRESCRIPTIONS
   --------------------------------------------------------- */

.prescription-table-wrapper {

    width: 100%;
    max-width: 100%;

    overflow-x: auto;
    overflow-y: visible;

    -webkit-overflow-scrolling: touch;

    box-sizing: border-box;
}


.prescription-table-wrapper .prescription-entry-table {

    min-width: 760px;
}


@media (max-width: 700px) {

    .prescription-table-wrapper {

        width: 100%;
        max-width: 100%;

        overflow-x: auto;
        overflow-y: visible;

        -webkit-overflow-scrolling: touch;
    }


    .prescription-table-wrapper .prescription-entry-table {

        min-width: 700px;
    }


    .prescription-table-wrapper th,
    .prescription-table-wrapper td {

        padding: 7px 8px;
        font-size: 12px;
        line-height: 1.3;
    }


    /*
       Consultation prescription medication table
    */

    .prescription-items {

        width: 100%;
        max-width: 100%;

        min-width: 0;

        overflow-x: auto;
        overflow-y: visible;

        -webkit-overflow-scrolling: touch;

        box-sizing: border-box;
    }


    .prescription-items .clinical-prescription-table {

        min-width: 700px;
    }


    .prescription-items::after {

        content: "? Swipe or scroll right to see more";

        display: block;

        width: 100%;
        box-sizing: border-box;

        margin-top: 6px;
        padding: 6px 8px;

        text-align: center;

        font-size: 11px;
        line-height: 1.3;

        color: #555;
        background: #f5f5f5;

        border-radius: 5px;
    }


    .prescription-items .clinical-prescription-table th,
    .prescription-items .clinical-prescription-table td {

        padding: 7px 8px;
        font-size: 12px;
        line-height: 1.3;
    }
}


/* ---------------------------------------------------------
   LAB REQUESTS
   --------------------------------------------------------- */

#labRequestsHistory {

    width: 100%;
    max-width: 100%;

    min-width: 0;
}


#labRequestsHistory > div:has(> .consultation-edit-lab-request-table) {

    width: 100%;
    max-width: 100%;

    overflow-x: auto;
    overflow-y: visible;

    -webkit-overflow-scrolling: touch;

    box-sizing: border-box;
}


@media (max-width: 700px) {

    #labRequestsHistory > div:has(> .consultation-edit-lab-request-table) {

        width: 100%;
        max-width: 100%;

        overflow-x: auto;
        overflow-y: visible;

        -webkit-overflow-scrolling: touch;
    }


    #labRequestsHistory
    .consultation-edit-lab-request-table {

        min-width: 760px;
    }


    #labRequestsHistory
    .consultation-edit-lab-request-table th,
    #labRequestsHistory
    .consultation-edit-lab-request-table td {

        padding: 7px 8px;

        font-size: 12px;
        line-height: 1.35;

        vertical-align: top;
    }


    #labRequestsHistory
    > div:has(> .consultation-edit-lab-request-table)::after {

        content: "? Swipe or scroll right to see more";

        display: block;

        width: 100%;
        box-sizing: border-box;

        margin-top: 6px;
        padding: 6px 8px;

        text-align: center;

        font-size: 11px;
        line-height: 1.3;

        color: #555;
        background: #f5f5f5;

        border-radius: 5px;
    }
}


/* ---------------------------------------------------------
   LAB RESULTS
   --------------------------------------------------------- */

.lab-table-wrapper,
.clinical-lab-report-table-wrapper {

    width: 100%;
    max-width: 100%;

    overflow-x: auto;
    overflow-y: visible;

    -webkit-overflow-scrolling: touch;

    box-sizing: border-box;
}


@media (max-width: 700px) {

    .lab-table-wrapper .lab-results-table {

        min-width: 620px;
    }


    .lab-table-wrapper .lab-results-table th,
    .lab-table-wrapper .lab-results-table td {

        padding: 7px 8px;

        font-size: 12px;
        line-height: 1.35;

        vertical-align: top;
    }


    .lab-table-wrapper::after {

        content: "? Swipe or scroll right to see more";

        display: block;

        width: 100%;
        box-sizing: border-box;

        margin-top: 6px;
        padding: 6px 8px;

        text-align: center;

        font-size: 11px;
        line-height: 1.3;

        color: #555;
        background: #f5f5f5;

        border-radius: 5px;
    }


    .clinical-lab-report-table-wrapper::after {

        content: "? Swipe or scroll right to see more";

        display: block;

        width: 100%;
        box-sizing: border-box;

        margin-top: 6px;
        padding: 6px 8px;

        text-align: center;

        font-size: 11px;
        line-height: 1.3;

        color: #555;
        background: #f5f5f5;

        border-radius: 5px;
    }
}


/* ---------------------------------------------------------
   EXTRA SMALL PHONES
   --------------------------------------------------------- */

@media (max-width: 480px) {

    .clinical-vitals-horizontal .clinical-vitals-table th,
    .clinical-vitals-horizontal .clinical-vitals-table td,

    .prescription-items .clinical-prescription-table th,
    .prescription-items .clinical-prescription-table td,

    #labRequestsHistory
    .consultation-edit-lab-request-table th,
    #labRequestsHistory
    .consultation-edit-lab-request-table td,

    .lab-table-wrapper .lab-results-table th,
    .lab-table-wrapper .lab-results-table td {

        padding: 6px 7px;
        font-size: 11px;
    }


    .clinical-vitals-horizontal::after,
    .prescription-items::after,
    #labRequestsHistory
    > div:has(> .consultation-edit-lab-request-table)::after,
    .lab-table-wrapper::after,
    .clinical-lab-report-table-wrapper::after {

        font-size: 10px;
        padding: 5px 6px;
    }
}

</style>

<style>" +

        "@page {" +
            "size: A4;" +
            "margin: 14mm;" +
        "}" +

        "* {" +
            "box-sizing: border-box;" +
        "}" +

        "html, body {" +
            "margin: 0;" +
            "padding: 0;" +
            "background: #fff;" +
            "color: #222;" +
            "font-family: Arial, Helvetica, sans-serif;" +
            "font-size: 12px;" +
        "}" +

        "body {" +
            "width: 100%;" +
        "}" +

        ".lab-request-header {" +
            "text-align: center;" +
            "border-bottom: 2px solid #222;" +
            "padding-bottom: 12px;" +
            "margin-bottom: 18px;" +
        "}" +

        ".lab-request-header h1 {" +
            "margin: 0;" +
            "font-size: 24px;" +
        "}" +

        ".lab-request-header p {" +
            "margin: 5px 0 10px;" +
            "font-size: 12px;" +
        "}" +

        ".lab-request-header h2 {" +
            "margin: 0;" +
            "font-size: 18px;" +
        "}" +

        ".lab-request-patient-section," +
        ".lab-request-section {" +
            "margin-bottom: 18px;" +
        "}" +

        ".lab-request-patient-section h3," +
        ".lab-request-section h3 {" +
            "margin: 0 0 8px;" +
            "font-size: 14px;" +
            "border-bottom: 1px solid #aaa;" +
            "padding-bottom: 4px;" +
        "}" +

        ".lab-request-patient-grid {" +
            "display: grid;" +
            "grid-template-columns: 1fr 1fr;" +
            "gap: 8px 20px;" +
        "}" +

        ".lab-request-tests {" +
            "display: grid;" +
            "grid-template-columns: 1fr 1fr;" +
            "gap: 7px 15px;" +
        "}" +

        ".lab-request-tests label {" +
            "display: block;" +
        "}" +

        ".lab-request-tests input {" +
            "margin-right: 6px;" +
        "}" +

        ".form-group {" +
            "margin-top: 10px;" +
        "}" +

        ".form-group label {" +
            "display: block;" +
            "font-weight: bold;" +
            "margin-bottom: 5px;" +
        "}" +

        "input[type='text']," +
        "textarea {" +
            "width: 100%;" +
            "border: 1px solid #999;" +
            "padding: 7px;" +
            "font-family: Arial, Helvetica, sans-serif;" +
            "font-size: 12px;" +
            "background: #fff;" +
        "}" +

        "textarea {" +
            "min-height: 70px;" +
            "resize: none;" +
        "}" +

        ".lab-request-signature {" +
            "display: flex;" +
            "flex-direction: column;" +
            "align-items: flex-start;" +
            "margin-top: 20px;" +
        "}" +

        ".signature-line {" +
            "border-bottom: 1px solid #222;" +
            "height: 28px;" +
            "margin-top: 5px;" +
        "}" +

        ".lab-request-footer {" +
            "margin-top: 40px;" +
            "padding-top: 10px;" +
            "border-top: 1px solid #aaa;" +
            "text-align: center;" +
            "font-size: 10px;" +
            "line-height: 1.5;" +
        "}" +

        ".no-print," +
        "button," +
        ".close-modal {" +
            "display: none !important;" +
        "}" +

        "</style>


